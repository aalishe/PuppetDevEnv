---
:backends:
  - yaml

:logger: console

:hierarchy:
  - "nodes/%{::fqdn}"
  - "roles/%{::application_name}/%{::node_type}"
  - nodes/default
  - "%{calling_module}/data/defaults"
  - global

:yaml:
  :datadir: "/vagrant/puppet/environments/%{::environment}/hieradata"
